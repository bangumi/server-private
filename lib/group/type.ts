export enum GroupTopicMode {
  All = 'all',
  Joined = 'joined',
}

export enum GroupSort {
  Members = 'members',
  Posts = 'posts',
  Topics = 'topics',
  Created = 'created',
  Updated = 'updated',
}
